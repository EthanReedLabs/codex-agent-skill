# 任务书骨架（复制后填空，<> 为填空位）

## 目标
<一句话说清要达成什么>

## 范围
涉及路径：<明确列出>
禁止改动：破坏性 git 操作；删除非任务范围文件；修改全局配置；<任务特定项>

## 已知上下文
先读 <项目根>/.codex-agent/PROJECT-MAP.md 获取项目结构，只对与本任务相关的部分做验证性探索。
<Claude 已掌握的其他信息：关键代码片段、上一轮结论；有历史日志给路径不贴内容>

## 执行效率要求
探索命令尽量合并为单条复合命令（for 循环批处理）——每条命令是一次约 18 秒的模型往返。
若发现 PROJECT-MAP.md 与实际不符，顺手修正它（仅 workspace-write 轮）。
<机械成文类加：one-shot——零探索零自验，直接产出目标文件；验证由指挥方全权负责>


## 日志文件
本工作流日志：<项目根>/.codex-agent/<slug>.md
<按类型三选一：
 T1（只读）→ 完整汇报放最终消息，由指挥方落盘；
 T2/T3 非 one-shot（含 exec 通道）→ 每轮结束将完整汇报自行追加写入该文件；
 one-shot → 产物即汇报，最终消息一行确认，免日志（指挥方轻量簿记）>

## 完成标准
<可逐条验证的条目，每条须附原始证据：命令原文 + exit code + 输出尾行>

## 体量与熔断
本轮任务体量控制在约 10 分钟内可完成。
<T3 专用：测试修错尝试 3 次仍未通过 → 停止并汇报当前状态>
